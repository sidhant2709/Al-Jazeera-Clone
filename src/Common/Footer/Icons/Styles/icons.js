import styled from 'styled-components';

export const Icon = styled.i`
    font-size:30px;
    margin-right:35px;
    cursor: pointer;
    &:hover{
        color:#797878;
        transition:0.5s ease-in-out;
    }
`